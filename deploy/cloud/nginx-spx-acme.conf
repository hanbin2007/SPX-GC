# Temporary HTTP-only vhost used while obtaining the first TLS certificate.
server {
    listen 80;
    server_name spx.origenclub.cn;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/acme;
        try_files $uri =404;
    }

    location / { return 404; }
}
